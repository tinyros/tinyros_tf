include_directories(.)

aux_source_directory(. SOURCE_FILES)
aux_source_directory(markers SOURCE_FILES)
aux_source_directory(../utils SOURCE_FILES)
aux_source_directory(../ogre_tools SOURCE_FILES)
add_definitions(-DBOOST_LOG_DYN_LINK)
include_directories(${Boost_INCLUDE_DIRS})
add_library(default_plugin ${SOURCE_FILES})                              
target_link_libraries(default_plugin ${wxWidgets_LIBRARIES} ${OGRE_LIBRARIES})
target_link_libraries(default_plugin boost_thread boost_signals boost_filesystem boost_system)
